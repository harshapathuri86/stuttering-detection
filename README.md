### Run frontend

In folder `frontend` run:

```bash
yarn start
```

 

### Run backend

> Make sure you have docker installed in your system

In root folder, run:

``` bash
docker-compose build
docker-compose up
```

